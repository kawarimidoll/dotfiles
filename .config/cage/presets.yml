presets:
  base:
    allow:
      - "."
      - "$XDG_CACHE_HOME" # ~/.cache
      - "$XDG_DATA_HOME" # ~/.local/share
      - "/private/tmp" # macos tmp directory
      - "/var/folders" # macos mktemp
      - "/private/var/folders" # macos mktemp
      - "/Library/Caches" # macos app caches
      - "$HOME/Library/Caches" # macos app caches
      - "/dev" # pty

  npm:
    allow:
      - "$HOME/.npm"
      - "$HOME/.npmrc"
      - "$HOME/.config/.wrangler"

  git-enabled:
    allow-git: true
    allow-keychain: true # macOS only

  claude-code:
    allow:
      - "$CLAUDE_CONFIG_DIR"
      - "$HOME/.serena"
      - "$HOME/.agent-browser"
      - "$HOME/.browser-profile"
      - "$HOME/.codex"
      - ".serena"

  codex:
    allow:
      - "$HOME/.serena"
      - "$HOME/.agent-browser"
      - "$HOME/.browser-profile"
      - "$HOME/.codex"
      - ".serena"

  happy:
    allow:
      - "$HOME/.happy"

auto-presets:
  - command: claude
    presets:
      - base
      - git-enabled
      - npm
      - claude-code

  - command: happy
    presets:
      - base
      - git-enabled
      - npm
      - claude-code
      - happy

  - command: git
    presets:
      - base
      - git-enabled
